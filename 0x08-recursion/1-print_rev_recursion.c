#include "main.h"

/**
 * _print_rev_recursion - print reverse function using recursion in reverse
 * @s: input char
 */

void _print_rev_recursion(char *s)
{

f (*s != '\0')
{
_print_rev_recursion(s + 1);
_putchar(*s);
}
}
