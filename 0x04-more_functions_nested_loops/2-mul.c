#include "main.h"

/**
* mul - multiply
* Description: multiplies two numbers
* @a: first value
* @b: second value
* Return: the multiplication value of the two number
*/

int mul(int a, int b)
{
return (a * b);
}
