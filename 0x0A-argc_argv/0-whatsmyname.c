#include <stdio.h>
#include <stdio.h>

/**
 * main - this program just print the name of the file using the argv
 * @argc: argument count
 * @argv: argument variable array whichvis same size as argc
 * Return: 0 for success
 */

int main(int argc, char* argv[])
{
printf("%s\n", argv[0]);
return (0);
}
