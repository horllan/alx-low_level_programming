#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
 * main - starting point
 * Description: checks for alphabet
 * Return: 1 for true and 0 for false
 */

int main(void)
{
int r;
r = _isalpha(c);
putchar(r +'0');
putchar('\n');
return (0);
}
